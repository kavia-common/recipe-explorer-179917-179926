# API base URL for the backend. Defaults to '/api' if not set.
# You can also set REACT_APP_BACKEND_URL as a fallback.
REACT_APP_API_BASE=https://your-backend.example.com/api

# Development server port is 3000 by default (set in package.json start script).
# REACT_APP_PORT=3000
